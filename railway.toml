[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c \"gunicorn config.wsgi:application --bind 0.0.0.0:$PORT\""
healthcheckPath = "/health/"
healthcheckTimeout = 25
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
